# 生データ（クライアント数値を含むため、平文ではコミットしない）
data/dashboard.json
dist/

# 認証情報（絶対にコミットしない）
.env
*.env
credentials*.json
*.pem
*.key

# OS・エディタ
.DS_Store
desktop.ini
__pycache__/
*.pyc
*.bak

# HubSpotリード数（クライアント数値のためコミットしない。Actions側はSecret HUBSPOT_LEADS_JSONを使う）
data/hubspot_leads.json
